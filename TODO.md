# TODO

- [x] Persist data between uses, even if the browser window is closed. Since offline support has to be a priority, no database services allowed.
- [x] For testing only, add a "clear data" button at the bottom of the app which will reset all stored data.
- [x] Include timers for each player on the team to track how much playtime they have had during the current game.
- [x] Create a mechanism for "substitution" — when subs are actually put onto the field, the "pending" lineup becomes the active lineup and a new blank "pending" lineup is created.
